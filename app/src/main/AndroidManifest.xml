<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/nosteq_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nosteq_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nosteq"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UpdateDialog"
            android:exported="false"
            android:label="@string/title_activity_update_dialog"
            android:theme="@style/Theme.Nosteq" />
        <activity
            android:name=".MpesaConfigScreen"
            android:exported="false"
            android:label="@string/title_activity_mpesa_config_screen"
            android:theme="@style/Theme.Nosteq" />
        <activity
            android:name="com.kevannTechnologies.nosteqCustomers.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Nosteq">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.kevannTechnologies.nosteqCustomers.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Nosteq" />

        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>